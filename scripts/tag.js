*function toArray(tags) {
    for (var i = 0; i < tags.length; i++) tagsArray.push(tags[i]);
  
    return tagsArray;
  }
  console.log(tagsArray);
  /*tags.addEventListener("clic", insertTag);
  
  function insertTag() {
    const tagsArray = [];
  
    tags.forEach((tag) => {});
  }*/
